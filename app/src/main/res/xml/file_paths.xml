<?xml version="1.0" encoding="utf-8"?>
<!-- Наружу отдаются два подкаталога кэша: скачанные вложения, которые FileStore отдаёт чужому
     приложению, и снимки, которые камера кладёт нам. Всё остальное, включая токены и базы, для
     чужих приложений не существует. -->
<paths>
    <cache-path name="shared" path="shared/" />
    <cache-path name="camera" path="camera/" />
</paths>
