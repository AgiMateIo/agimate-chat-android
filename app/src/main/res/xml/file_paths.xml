<?xml version="1.0" encoding="utf-8"?>
<!-- Наружу отдаётся ровно один подкаталог кэша — тот, куда FileStore кладёт скачанные вложения.
     Всё остальное, включая токены и базы, для чужих приложений не существует. -->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
