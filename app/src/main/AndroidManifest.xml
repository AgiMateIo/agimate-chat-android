<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- То же разрешение приносит манифест пуш-SDK; объявлено и здесь, чтобы список того, что
         приложение просит у человека, читался в одном месте. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Сохранение вложения в галерею и «Загрузки». С Android 10 это делается через MediaStore и
         разрешения не требует вовсе — отсюда maxSdkVersion. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Видимость пакетов для Custom Tabs: без этого на API 30+ браузер не находится. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>

        <!-- Съёмка вложения. Запустить интент видимость пакетов не требует, а вот спросить, есть
             ли кому его принять, — требует: без этого пункт «сделать фото» пропал бы там, где
             камера есть. -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:name=".AgiMateApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.AgiMate">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AgiMate"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Возврат из Custom Tabs по схеме. Фолбэк к App Link: схему может зарегистрировать
                 на себя чужое приложение, поэтому PKCE обязателен. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="${authHost}"
                    android:scheme="${authScheme}" />
            </intent-filter>
        </activity>

        <!-- Чужому приложению отдаётся не подписанная ссылка на файл, а копия в кэше: ссылка живёт
             пятнадцать минут и ведёт в наш контур. Список каталогов — в res/xml/file_paths.xml. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
